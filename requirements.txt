# Messaging + storage
aiokafka>=0.10
clickhouse-connect>=0.7
python-dotenv>=1.0
aiohttp
# Security scanners
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
sentence-transformers>=3.0   # pulls torch (semantic-attack classifier)
numpy>=1.26
